{
  "name": "docker-app",
  "version": "1.0.0",
  "description": "My first Docker app",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo 'Running unit test' && exit 0",
    "build": "mkdir -p build && echo 'Build Success' > build/index.html"
  },
  "dependencies": {
    "cross-spawn": "^7.0.5",
    "express": "^4.22.1",
    "glob": "^10.5.0"
  }
}
